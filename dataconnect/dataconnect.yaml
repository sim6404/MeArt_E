specVersion: "v1"
serviceId: "meart"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "meart-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
